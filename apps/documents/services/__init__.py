"""Infrastructure services for the documents app.

This subpackage contains service classes that coordinate between the
domain layer and external systems such as the ORM, file system,
and third-party libraries (e.g. Pandas, Word API). Examples
include the data-object interface to NoSQL storage and the
document renderer for producing DOCX files.
"""
